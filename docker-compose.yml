services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - CACHE_TTL=300
      - LOG_LEVEL=INFO
      - COOKIES_FILE=/cookies/cookies.txt
    volumes:
      - ./cookies:/cookies:ro
    restart: always
